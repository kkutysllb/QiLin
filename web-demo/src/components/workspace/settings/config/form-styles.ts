/** 表单 label 统一样式（原各 settings-form 内重复定义的 `labelCls`）。 */
export const labelCls = "text-sm font-medium leading-none";

/** 表单提示文案统一样式（原各 settings-form 内重复定义的 `hintCls`）。 */
export const hintCls = "mt-0.5 text-xs text-muted-foreground";
