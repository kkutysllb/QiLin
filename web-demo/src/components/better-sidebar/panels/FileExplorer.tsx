// PLACEHOLDER — replaced by the real FileExplorer panel in Task 5.
export function FileExplorerPanel({ scope, root }: { scope: { threadId: string }; root?: string }) {
  return <div className="p-2 text-xs text-muted-foreground">FileExplorer placeholder (Task 5)</div>;
}
