<!-- Generated by scripts/gen-kylin-catalog.ts — do not edit by hand.
     Run `pnpm run gen-kylin-catalog` to regenerate. -->

# Inherited Cordis API

The framework `ctx` members and events every plugin sees beyond the harness tier — pinned vendor source ([vendoring policy](../../vendor/README.md)), summarized tersely so the harness pages stay focused on repository-owned vocabulary. Detailed Context, Fiber, Registry, and Service APIs are generated in [context.md](context.md), [fiber.md](fiber.md), [registry.md](registry.md), and [service.md](service.md); the event-dispatch methods in [events.md](events.md).

This file is GENERATED from source (`scripts/gen-kylin-catalog.ts`) and verified fresh by `pnpm run verify-kylin-catalog` (part of `doc-sync`) — do not edit it by hand. Signature blocks use a `ts cordis-catalog` fence and include the original source JSDoc immediately before each event or service method. doc-typecheck skips these bare declaration fragments; type names in a signature link to the page that documents them.

## Inherited `ctx` members (kylin core + loader/hmr/timer)

- `ctx.on / ctx.once` — Register an event listener (disposable). ([`vendor/kylin/src/events.ts:34`](../../vendor/kylin/src/events.ts))
- `ctx.emit / ctx.parallel / ctx.serial / ctx.bail / ctx.waterfall` — Dispatch an event (sync / awaited / first-bail / short-circuit chain). ([`vendor/kylin/src/events.ts:34`](../../vendor/kylin/src/events.ts))
- `ctx.plugin / ctx.inject` — Load a plugin / declare required services. ([`vendor/kylin/src/registry.ts:164`](../../vendor/kylin/src/registry.ts))
- `ctx.effect` — Register a disposable side effect tied to the fiber. ([`vendor/kylin/src/fiber.ts:9`](../../vendor/kylin/src/fiber.ts))
- `ctx.get / ctx.set / ctx.provide / ctx.accessor / ctx.mixin` — Low-level service-store access and binding. ([`vendor/kylin/src/reflect.ts:7`](../../vendor/kylin/src/reflect.ts))
- `ctx.extend / ctx.isolate / ctx.intercept` — Derive a child context (scoped services / isolation / interception). ([`vendor/kylin/src/context.ts:42`](../../vendor/kylin/src/context.ts))
- `ctx.root / ctx.fiber / ctx.registry / ctx.reflect / ctx.events / ctx.logger` — Ambient handles onto the running context graph. ([`vendor/kylin/src/context.ts:16`](../../vendor/kylin/src/context.ts))
- `ctx.timer (+ interval / timeout / throttle / debounce)` — Disposable timer helpers. The `timer` key is provided at runtime; the four supported helpers are mixed onto ctx directly (declared via Pick). ([`vendor/timer/src/index.ts:4`](../../vendor/timer/src/index.ts))
- `ctx.loader` — The config Loader that booted the app (present under the loader). ([`vendor/loader/src/index.ts:30`](../../vendor/loader/src/index.ts))

## Inherited events (kylin core + loader/hmr/timer)

- `internal/plugin` — A plugin fiber was created. ([`vendor/kylin/src/events.ts:328`](../../vendor/kylin/src/events.ts))
- `internal/status` — A fiber changed lifecycle state. ([`vendor/kylin/src/events.ts:330`](../../vendor/kylin/src/events.ts))
- `internal/service` — Interception hook for a service binding (no core producer). ([`vendor/kylin/src/events.ts:332`](../../vendor/kylin/src/events.ts))
- `internal/update` — Waterfall: a fiber config update is being applied. ([`vendor/kylin/src/events.ts:334`](../../vendor/kylin/src/events.ts))
- `internal/get` — Waterfall: a service is being read from the store. ([`vendor/kylin/src/events.ts:336`](../../vendor/kylin/src/events.ts))
- `internal/set` — Waterfall: a service is being written to the store. ([`vendor/kylin/src/events.ts:338`](../../vendor/kylin/src/events.ts))
- `internal/listener` — A listener was registered. ([`vendor/kylin/src/events.ts:340`](../../vendor/kylin/src/events.ts))
- `internal/dispatch` — An event is being dispatched to listeners. ([`vendor/kylin/src/events.ts:342`](../../vendor/kylin/src/events.ts))
- `exit` — The process is exiting on a signal. ([`vendor/loader/src/index.ts:23`](../../vendor/loader/src/index.ts))
- `loader/config-update` — The loader config tree changed. ([`vendor/loader/src/index.ts:24`](../../vendor/loader/src/index.ts))
- `loader/entry-init` — A config entry is being initialized. ([`vendor/loader/src/index.ts:25`](../../vendor/loader/src/index.ts))
- `loader/partial-dispose` — An entry is being partially disposed on reload. ([`vendor/loader/src/index.ts:26`](../../vendor/loader/src/index.ts))
- `loader/patch-context` — A context is being patched during a reload. ([`vendor/loader/src/index.ts:27`](../../vendor/loader/src/index.ts))
