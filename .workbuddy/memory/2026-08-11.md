# 2026-08-11

## qilin v2.0.0 发布（本地构建 + GitHub Release）

- 背景：代码已升级至 v2.0.0（HEAD `720a9c5`），但 `dist/` 仍为 1.0.0 产物，`v2.0.0` tag 未打。
- 操作：
  1. 用隔离 venv（`~/.workbuddy/binaries/python/envs/build`）安装 build + hatchling，构建 wheel + sdist 到 `dist/`；
  2. 验证 wheel 含 `qilin/`（539 项）与 `app/` 服务面（91 个文件：gateway/channels/scheduler）；
  3. 删除旧的 1.0.0 产物（dist/ 被 gitignore）；
  4. 打 annotated tag `v2.0.0`（"v2.0.0 · QiLin Multi-Agent Framework Release"）并推送 origin；
  5. `gh release create v2.0.0`，附上 whl + tar.gz，notes 取自 RELEASE_NOTES_v2.0.0.md。
- 结果：https://github.com/kkutysllb/QiLin/releases/tag/v2.0.0（正式版，非 draft）。
- 备注：无 PyPI 凭据（无 ~/.pypirc），未上传 PyPI；如需 PyPI 发布需用户提供 API token。
