# SDK-profile patch for the deterministic diagnostic child runtime.

- id: llm-deepseek
  name: '@qilin/llm-deepseek'
  disabled: true

- id: agent-instructions
  name: '@qilin/agent-instructions'
  disabled: true

- id: system-prompt
  name: '@qilin/system-prompt'
  config:
    personaPrefix: 'Return the scripted QILIN SDK failure.'

- insert:
    - id: child-mock-llm
      name: '../../../packages/subagent/subagent-qilin-sdk/tests/fixtures/loader/child-mock-llm.ts'
