- insert:
    - id: mcp-pagination-limit
      name: '@qilin/mcp-client'
      config:
        transport: stdio
        serverName: pagination-limit
        command: !!js process.execPath
        args: !!js '[process.env.QILIN_MCP_PAGINATION_FIXTURE]'
        failOnStartupError: true
        reconnect:
          enabled: false
