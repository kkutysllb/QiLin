/**
 * The one request path that serves the harness application document. The site
 * root keeps the public landing page and the sign-in screens have their own
 * documents, so every entry URL, redirect, and deployment index path names
 * this constant instead of repeating the literal.
 */

/** Request path serving the harness application document. */
export const WEB_ENTRY_PATH = '/workspace'
